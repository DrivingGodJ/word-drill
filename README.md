# WordDrill · 背单词

一个只为自己做的单词训练器。核心是一条规矩：**先认得出，再写得出。**
词从「未开始 → 认词中 → 拼写中 → 已掌握」逐级推进，没过前一关不会让你去拼它。
而且**一晚上刷满不算数** —— 每关都要在一个月内有足够多的**日子**答对过才放行（见「记忆模型」）。

纯静态站点。**默认**进度只存在本机浏览器、不上传；想手机电脑一起用，可以开「多设备同步」（见下文）。

## 怎么用

直接打开 `index.html` 所在的站点即可（首次使用建议「添加到主屏幕」，可离线）。

- **训练** — 点「开始训练」。选择题用键盘 `1`–`4` 作答，`Enter` 进入下一题。
  答错的词会在本轮稍后重现；答对的当天只记这一次，**过 20 分钟它会自己回来**，想多刷几遍就再练。
  练完点「再来一轮」会立刻把今天练过的词重新带上，不用等冷却。
- **词库** — 看熟练度分布、按阶段筛选、搜索单词。每条显示「已攒 x / 15 天」、对错次数和下次复习时间。
- **设置** — 每日新词上限、升阶所需答对天数（一个月内）、发音开关、多设备同步、进度导出/导入/清空。

## 多设备同步

进度存进一个**私有** GitHub 仓库，每台设备打开时拉取合并、练完自动推回。

### 为什么是「另一个私有仓库」而不是本仓库

本站是公开仓库。如果把进度写进这里，等于把单词表和学习进度公开挂在网上；
更麻烦的是，浏览器里那枚 token 会同时拥有**改写本站内容**的权限。所以进度单独放一个私有仓库，
token 只授权那一个仓库 —— 万一泄露也碰不到站点。

### 一次性配置

1. 建一个私有仓库放进度（例如 `worddrill-data`），随便放个 `README.md` 让它先有 `main` 分支。
2. 建一枚 **fine-grained PAT**（GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens）：
   - **Repository access** 选 *Only select repositories* → 只勾 `worddrill-data`
   - **Permissions** → Repository permissions → **Contents: Read and write**（其余全留 No access）
   - 设一个有效期（例如 90 天，到期重发一枚即可）
   - 生成后复制 `github_pat_…` 开头的串
   > 别用勾了全部仓库的经典 token —— 那种一旦泄露会影响你所有仓库。
3. 在设置页「多设备同步」里填：所有者、私有仓库名、进度文件路径（默认 `progress.json`）、token，打开开关。
4. 其余设备重复第 3 步（同一枚 token 可多处使用，也可各自建一枚）。

### 合并规则

不是整包覆盖，所以两台设备各练各的词不会互相丢：

- **每个词**：阶段 / 强度 / 下次复习时间取「最后练习时间（`lastSeen`）」较新的那条 —— 位置以最近练过的那台设备为准
- **每个词的对错次数**：取两端**较大值**而不是相加 —— 两台设备可能从同一份基线各自练习，相加会重复计数
- **学习参数**（每日新词上限、升阶判级）：按最后修改时间取新的
- **学习参数只在语义版本一致时同步**：存档格式升级（`STORE_VERSION`）会改变 `threshold` 的含义，
  旧版本客户端写上去的数值一律不采纳，免得把本机刚迁移好的新默认值顶掉
- **主题、发音**：设备偏好，不同步，各设备自己存
- **自动跳过**：两端内容完全一致时不会产生空提交，避免刷屏
- **并发写**：写入时带上读取到的文件 `sha`；若期间别的设备改过，GitHub 会返回 409，客户端会重读、重合并、重试（最多 3 轮）

### 安全说明

- token 只存在**这台设备**的浏览器（`localStorage`）里，只会发往 `api.github.com`。
- 点「清除凭据」只清本机的仓库地址与 token，不删远端进度、也不动学习记录。
- 清浏览器数据会连 token 一起清掉，届时重新填一次即可。

### 同步失败怎么办

先点设置面板里的**「测试连接」**，它会按层探测并直接说出卡在哪一步：
仓库看不看得见 → 有没有写权限 → 分支在不在 → 进度文件能不能读。

最常见的坑：**报 404 时，八成不是路径写错，而是 token 没授权这个仓库。**
GitHub 对私有仓库在「无权访问」和「不存在」两种情况下都返回 `404`（不是 `403`），
所以状态码本身分不出来。列出几种真实原因：

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 404 | 细粒度 token 的 *Repository access* 没勾上这个私有仓库 | 编辑 token，勾上该仓库 |
| 404 | 细粒度 token 建的时候选了 *Public repositories* | 改成 *Only select repositories* 并勾上 |
| 404 | 用了经典 token 但没勾 `repo` scope | 换细粒度 token，或补 `repo` 权限 |
| 404 | 所有者 / 仓库名拼错（注意 `worddrill-data` 中间有连字符） | 直接粘贴仓库网址，会自动拆开 |
| 404 | 分支名不对 | 测试连接会指出分支不存在；默认是 `main` |
| 403 | token 有仓库但没有 Contents 写权限，或被限流 | 改成 *Read and write*，或稍后重试 |
| 401 | token 过期 / 复制不全 | 重新生成一枚 |

「测试连接」通过后如果同步仍失败，再看提示里的状态码对照上表。

> token 改权限后**不需要**重新粘贴 —— 就地改权限即可，因为 token 串本身没变。

## 记忆模型

核心不是「当场答对几次」，而是**在多少个不同的日子答对过** —— 一晚上刷再多遍也不算记住。

| 阶段 | 出题方式 | 过关条件 |
| --- | --- | --- |
| 未开始 | 认词（四选一） | 首次答对即进入「认词中」 |
| 认词中 | 认词（四选一） | **一个月内**有 `threshold` 天答对过 → 「拼写中」 |
| 拼写中 | 拼写（看中文写英文） | **一个月内**有 `threshold` 天答对过 → 「已掌握」 |
| 已掌握 | 拼写 / 例句填空 | 答错即退回「拼写中」，已攒天数清零 |

- `threshold` 默认 **15**，可在设置里调到 1–30。两关都要达标，所以升到「已掌握」
  最快也要 **30 天**（每关 15 天，且天天都练、一次都不错）。
- **一天只记一次**：不论当天刷多少遍，`credits` 里最多留当天的第一条记录。
  判定用的是「哪几天答对过」，不是「答对过几遍」。
- **一天内可以反复刷**：答对后该词进入 `CREDIT_DUE = 20 分钟` 的冷却，
  冷却过去就回到队列，当天想练几遍都行。会话小结点「再来一轮」会连今天练过的词一起带上，
  不用等冷却。已掌握的词除外 —— 它们按维护间隔走。
- **一个月窗口（`PASS_WINDOW = 30 天`）**：每次答对都会丢掉 30 天前的旧记录。
  也就是这 `threshold` 天必须落在最近一个月内，拖过头就攒不满。
- **答错 → 已攒天数全部清零**（含当天），重新计数一个月。
  也就是「当天答对过才记这一天，之后又答错就整个作废」。已掌握的词答错同时降回「拼写中」。
- 已掌握后不再需要攒天数，只做维护：每 3 天回访一次（`MASTERED_DUE`）；答错则退回拼写关重新攒。
- **改判定语义时要一起升级 `STORE_VERSION`**：`threshold` 的含义改过三次，
  旧值在新语义下都不等价，所以每次语义变更都把存档版本 +1 并强制重置成新默认值，
  同时把 `settingsAt` 顶到现在 —— 否则会被同步仓库里那条「旧语义、时间戳却不比本机旧」的值挡回去
  （v4 修的就是这个：设置里明明该显示 15 天，却被旧同步值按回 5 天）。

> 一个月 / 15 天 + 答错清零是很严的组合：**中途任何一次答错，前面攒的天数全部作废**。
> 想宽松一点就调低 `threshold`；窗口长度、冷却时间、回访间隔是 `app.js` 顶部的常量。

## 目录结构

```
index.html              页面骨架（图标全部内联 SVG，没有 emoji）
styles.css              设计系统：浅/深色主题变量、间距、焦点环、响应式
app.js                  全部逻辑（SRS 调度、出题、渲染、快捷键）
data/words.json         词库 —— 唯一需要维护的数据文件
scripts/               工具脚本：FlowUs → 词库同步、图标栅格化
manifest.webmanifest    PWA 清单
sw.js                   Service worker（静态资源 stale-while-revalidate）
icon.svg / icon-maskable.svg     圆角版图标（浏览器 tab、Android）
icon-square.svg                   满幅方形源文件（PNG 由它生成）
apple-touch-icon.png / icon-192.png / icon-512.png / icon-maskable-512.png
```

### 图标：为什么既有 SVG 又有 PNG

- **iOS 的「添加到主屏幕」只认 PNG**，读不了 manifest 里的 SVG，也读不了 `short_name`
  （主屏名字只认 `<meta name="apple-mobile-web-app-title">`）。所以要 `apple-touch-icon.png`（180×180）。
- iOS 会**自己按圆角裁切**图标，所以 `apple-touch-icon.png` 必须是**满幅方形且不透明**的 ——
  不能预先画圆角（否则圆角外套圆角，出现双重描边），也不能留透明（会渲染成黑块）。
- 改图标时别手改 PNG：改 `icon-square.svg` / `icon-maskable.svg` 的几何，
  然后 `python3 scripts/make-icons.py` 重新生成（纯标准库，4×4 超采样抗锯齿，无需 ImageMagick）。
- 图标有强缓存：**已加到主屏幕的旧图标不会自动更新**，要删掉重新添加一次。

### 深浅色：自动跟随系统

- 页面默认就是「跟随系统」：CSS 用 `@media (prefers-color-scheme: dark)` 实现自动切换，
  右上角按钮只是手动覆盖（跟随系统 → 深色 → 浅色循环），选中的值存在本机、不同步。
- `theme-color` 分浅/深两份（带 `media` 属性），Safari 顶栏 / Android 工具栏跟着系统走。
- iOS 主屏幕模式用的是 `black-translucent` 透明状态栏 + `viewport-fit=cover`，
  页面自己在顶部画一条 `--statusbar-bg`（浅色下是品牌紫、深色下是近黑），状态栏区域也就跟着系统变了。
  代价是状态栏文字固定为白色，所以这条底色两种主题都必须够深。
- 注意：`apple-mobile-web-app-status-bar-style` 在「添加到主屏幕」时被 iOS 缓存，
  改了它要**删掉主屏幕图标重新添加**才生效。


## 加新词（日常流程）

1. 在 FlowUs 的「生词」页按现有格式记下新词；
2. 跑同步脚本，把 FlowUs 拉到本地词库：

```bash
# 预览会改什么，不写盘
python3 scripts/sync-from-flowus.py --dry-run

# 真正同步（FlowUs 为准，本地已有的搭配/标签会保留）
python3 scripts/sync-from-flowus.py
```

3. 提交并推送，站点自动更新。

也可以直接编辑 `data/words.json`。每条的结构：

```json
{
  "id": "unveil",
  "word": "unveil",
  "pos": "v.",
  "meaning": "揭开，公布；为…揭幕",
  "example": "The company will unveil its new concept car next month.",
  "exampleZh": "该公司下月将发布新款概念车。",
  "collocations": ["unveil a plan"],
  "tags": []
}
```

> `id` 要唯一且稳定 —— 学习进度是按 `id` 索引的，改了 `id` 等于把它当成新词。

## 数据说明

浏览器本地有两个键：

| 键 | 内容 |
| --- | --- |
| `worddrill.v1` | 学习进度：每个词的阶段、强度、对错次数、下次复习时间 + 全局设置 |
| `worddrill.sync.v1` | 同步配置：仓库地址、token、本机标识、上次同步结果（只在开了同步后才有） |

换设备且不开同步时，用设置里的「导出进度 / 导入进度」搬运。清缓存会清掉进度（以及同步用的 token），词库不受影响。
